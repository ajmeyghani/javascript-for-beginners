console.clear();
/*
    Array: an object that can hold a list of items
*/

var numbers = [1,2,3,4];

// is an object.

numbers[0];

numbers.length; // 4

numbers.slice(2);

// numbers.splice(0, 1);

numbers.indexOf(4)


// other methods (in the function section)
